# ─── LegalWise Frontend Environment ─────────────────────────
# In development, leave VITE_API_BASE_URL empty — the Vite proxy
# forwards /api/* requests to the backend automatically.
#
# In production, set this to the backend origin:
#   VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
